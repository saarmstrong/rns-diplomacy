"""Order composition and submission from the client side.

Helps the player construct valid orders for their units and submit
them to the coordinator before the deadline.
"""

# TODO: Implement client-side order handling
#   - compose_order(unit, order_type, ...) -> Order
#   - validate_order_locally(order, state) -> bool
#   - submit_orders(orders) -> SubmissionResult
#   - List available moves for a unit
